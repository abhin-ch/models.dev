# Cache key (chat): none — chat completions + usage document no cache params or cached-token counters; prompt_cache_key is consumed at the gateway and removed before upstream.
# Cache headers: none
# Cache policy: server-side automatic prefix reuse; no cache billing.
# Cache reuse: retain full history; keep a stable append-only prefix.
# Docs: https://docs.poolside.ai/api/overview
name = "Poolside"
env = ["POOLSIDE_API_KEY"]
npm = "@ai-sdk/openai-compatible"
api = "https://inference.poolside.ai/v1"
doc = "https://platform.poolside.ai"
