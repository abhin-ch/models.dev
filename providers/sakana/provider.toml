# Cache key (chat): Use headers/body none — supported-fields list has no prompt_cache_key or cache_control; cached_tokens usage fields are billing evidence only.
# Cache headers: none
# Cache body: none
# Cache policy: auto — server-side prefix/orchestration reuse, no client control; keep stable prefix first, hits surface via cached_tokens usage.
# Docs: https://console.sakana.ai/models
name = "Sakana AI"
env = ["SAKANA_API_KEY"]
npm = "@ai-sdk/openai-compatible"
api = "https://api.sakana.ai/v1"
doc = "https://console.sakana.ai/models"
