# Cache key (chat): none — OpenAPI schema lists only model/messages/style/stream; no cache control documented; prompt_cache_key is consumed at the gateway and removed before upstream.
# Cache headers: none
# Cache policy: relay; caching (if any) follows the serving upstream.
# Cache reuse: retain full history with a stable prefix.
# Docs: https://www.qhaigc.net/docs/api-reference/chat/openai-style.md
name = "QiHang"
npm = "@ai-sdk/openai-compatible"
api = "https://api.qhaigc.net/v1"
env = ["QIHANG_API_KEY"]
# Reasoning HTTP format (accessed 2026-06-25):
# POST /v1/chat/completions is documented as OpenAI compatible, but the public
# request reference does not document a reasoning control or accepted values.
# Source:
# https://www.qhaigc.net/docs/api-reference/chat/openai-style.md
doc = "https://www.qhaigc.net/docs"
