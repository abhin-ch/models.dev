# Cache key (chat): strip prompt_cache_key — confirmed public interface documents only OpenAI-shaped model/messages/stream request; no cache params or cached-token usage published.
# Cache headers: none
# Cache policy: no documented prompt cache; identical-prefix retries rely on upstream implicit caching only.
# Docs: https://inferx.net/docs/api-reference
name = "InferX"
env = ["INFERX_API_KEY"]
npm = "@ai-sdk/openai-compatible"
api = "https://model.inferx.net/endpoints/v1"
doc = "https://model.inferx.net/endpoints"
