# Cache key (chat): Use headers/body none — public interface documents only OpenAI-shaped model/messages/stream request with no cache params.
# Cache headers: none
# Cache policy: no documented prompt cache; replay requests with a stable byte-identical prefix on the same model.
# Cache verify: no cached-token usage published — treat as uncached.
# Docs: https://inferx.net/docs/api-reference
name = "InferX"
env = ["INFERX_API_KEY"]
npm = "@ai-sdk/openai-compatible"
api = "https://model.inferx.net/endpoints/v1"
doc = "https://model.inferx.net/endpoints"
