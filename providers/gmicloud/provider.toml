# Cache key (chat): none — closed POST /v1/chat/completions table (model/messages/tools/max_tokens/temperature/top_p/top_k/ignore_eos/stop/response_format/stream) omits key; usage reports no cached fields.
# Cache headers: none (X-Organization-ID is org routing, not cache).
# Cache policy: no chat key or cache billing; server-side vLLM/SGLang prefix caching is automatic infra-level with no client control; keep stable prefix first.
# Cache verification: no cache usage fields or billing signal.
# Docs: https://docs.gmicloud.ai/inference-engine/api-reference/llm-api-reference
name = "GMI Cloud"
# Reasoning HTTP format (accessed 2026-06-25): POST /v1/messages for Claude.
# Native Messages uses thinking = { type = "enabled", budget_tokens = N };
# GMI examples do not document disable, effort values, or numeric bounds.
# https://docs.gmicloud.ai/model-quickstarts/text/anthropic-claude-opus-4-6
env = ["GMICLOUD_API_KEY"]
npm = "@ai-sdk/openai-compatible"
api = "https://api.gmi-serving.com/v1"
doc = "https://docs.gmicloud.ai/inference-engine/api-reference/llm-api-reference"
