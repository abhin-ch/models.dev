# Use headers: none
# Use body: none (no cache field in the v2/chat request body: model/messages/tools/documents/response_format/safety_mode/max_tokens/stop_sequences/temperature/seed/penalties/k/p/logprobs/tool_choice/thinking/priority/strict_tools)
# Prefix steps: keep a stable prompt prefix with identical model and params; repeat the full prefix on each request
# Behavior: automatic inference cache; hits surface as usage.cached_tokens with no client control
# Docs: https://docs.cohere.com/reference/chat
name = "Cohere"
env = ["COHERE_API_KEY"]
npm = "@ai-sdk/cohere"
# Reasoning HTTP format (accessed 2026-06-25):
# Native Chat: POST https://api.cohere.com/v2/chat. Toggle:
# thinking.type = "enabled"|"disabled". Budget: thinking.token_budget = a
# positive integer. Effort is not documented on this request surface.
# OpenAI Chat: POST https://api.cohere.ai/compatibility/v1/chat/completions.
# reasoning_effort = "none"|"high" maps to disabled|enabled thinking;
# token budget is not documented on this request surface.
# Sources:
# https://docs.cohere.com/reference/chat
# https://docs.cohere.com/docs/reasoning
# https://docs.cohere.com/docs/compatibility-api
doc = "https://docs.cohere.com/docs/models"
