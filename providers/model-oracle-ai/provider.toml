# Use headers: none
# Use body: none — decision-engine router documents no cache controls; only reasoning_effort is forwarded/translated to the selected upstream provider.
# Auto cache: none documented at the router; upstream cache applies where the selected provider supports it.
# Steps: none documented; keep the prefix stable for whatever upstream serves the request.
# Docs: https://modeloracle.com/setup/
# OpenAI-compatible endpoint and public model aliases:
# https://modeloracle.com/setup/ (accessed 2026-07-07)
# API contract and routing behavior:
# https://github.com/anomalyco/ai-control-plane/blob/main/docs/api.md
name = "Model Oracle AI"
env = ["MODEL_ORACLE_API_KEY"]
npm = "@ai-sdk/openai-compatible"
api = "https://api.modeloracle.com/api/v1"
doc = "https://modeloracle.com/setup/"
