# Prompt cache (chat): UNKNOWN — serverless OpenAI-compatible API; docs cover setup and basic chat only, no cache fields or caching behavior.
# Docs: https://docs.inceptron.io
name = "Inceptron"
npm = "@ai-sdk/openai-compatible"
env = ["INCEPTRON_API_KEY"]
# Per-model reasoning controls are synchronized from the public model catalog.
api = "https://api.inceptron.io/v1"
doc = "https://docs.inceptron.io"
