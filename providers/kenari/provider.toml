# Cache key (chat): Use headers/body none — provider site and docs unreachable (connection failed); third-party report confirms plain OpenAI-compatible chat completions with no cache params.
# Cache headers: none
# Cache policy: no documented prompt cache; replay requests with a stable byte-identical prefix on the same model.
# Cache verify: no cached-token usage documented — treat as uncached.
# Docs: https://kenari.id/docs
name = "Kenari"
npm = "@ai-sdk/openai-compatible"
env = ["KENARI_API_KEY"]
api = "https://kenari.id/v1"
doc = "https://kenari.id/docs"